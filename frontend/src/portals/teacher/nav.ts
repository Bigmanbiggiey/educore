export interface NavItem {
  label: string;
  path: string;
}

// Empty until this portal has more than one page — docs/roadmap.md Phase 1
// milestone is the dashboard shell only, no real nav yet.
export const navItems: NavItem[] = [];
