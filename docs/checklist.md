# Implementation Checklist

Tracks execution against `docs/roadmap.md`, phase by phase. Where a phase
touches Django apps, the build order matches the dependency order fixed in
`docs/modules.md` (Layer 0 → 1 → 2 → 3) — don't start an app before the ones
above it in its list are done.

Check an item only once it's merged to `master` and passing CI, not when
it's "basically working locally." Update the status table in
`docs/roadmap.md` when an entire phase's boxes are checked.

---

## Definition of Done — per Django app

Apply this to every app below before checking it off:

- [ ] Models defined (`TenantScopedModel`/`TimeStampedModel`/`SoftDeleteModel` from `core` where applicable)
- [ ] Migrations generated and applied cleanly on a fresh DB
- [ ] `services.py` (writes) and `selectors.py` (reads) implemented — no other app reaches into this app's ORM directly
- [ ] DRF serializers + viewsets + router registered under `/api/v1/`
- [ ] Admin registration where useful for support/ops
- [ ] Permission classes applied (`IsAuthenticated` + tenant scoping + role checks)
- [ ] Unit tests for `services`/`selectors`; integration tests for API endpoints
- [ ] `import-linter` contract passes — no illegal cross-layer imports
- [ ] Added to `LOCAL_APPS` in `backend/config/settings/base.py`
- [ ] OpenAPI schema renders cleanly via `drf-spectacular`

---

## Phase 0 — Scaffolding ✅ DONE

- [x] Django backend skeleton (config/apps split, DRF, JWT, Celery)
- [x] React/Vite/TS frontend skeleton (app shell, boundary-enforced structure, Tailwind v4)
- [x] Docker Compose stack (postgres/redis/minio/backend/celery/frontend/nginx) + dev/prod overrides
- [x] CI pipeline (ruff, import-linter, eslint, tsc, image builds)
- [x] Design docs: architecture, modules, database, api-design, authentication, permissions, multitenancy, deployment, project-structure, frontend-architecture
- [x] vision.md, roadmap.md, coding-standards.md, ui-guidelines.md

---

## Phase 1 — Platform Foundation (current phase)

### Layer 0 apps — build in this exact order

- [x] `core` — `TimeStampedModel`, `SoftDeleteModel`, `TenantScopedModel` (+ `TenantScopedSoftDeleteModel` composing both), UUIDv7 PK, `current_institution`/`correlation_id_ctx` context vars, `StandardPageNumberPagination`, `ConflictError`, `CorrelationIdMiddleware` + JSON logging filter. No API surface or migrations (abstract models only) — verified via ruff, `manage.py check`, `makemigrations --check`, `pytest` (12/12), `lint-imports`, and a live `/healthz/` request.
- [x] `institutions` — `Institution`, `InstitutionCurriculum`, `Domain`, `AcademicCalendarSettings`; `selectors.get_institution_by_domain`; `services.provision_institution`/`set_isolation_tier`/`add_custom_domain`/`verify_domain`; `TenantMiddleware`; `TenantDBRouter` (added to `core`, since it only duck-types `institution.isolation_tier`/`db_alias` and needs no import of `institutions`). `PLATFORM_HOSTS` setting added. Seeding the admin User/InstitutionMembership + welcome notification in `provision_institution`, and the live `upgrade_isolation_tier` runbook, are explicitly deferred until `accounts`/`permissions`/`notifications_core` exist. Verified via ruff, `manage.py check`, `makemigrations`/`migrate`, `pytest` (40/40 across `core`+`institutions`), `lint-imports` (new core↔institutions layer contract kept), and live requests confirming `/healthz/` still works and an unresolved tenant host 404s.
- [x] `accounts` — custom `User` (email/phone login, no username; UUIDv7 PK; `is_platform_staff` in place of Django's `is_staff`/`is_superuser`; MFA fields reserved), `PasswordResetToken`; `services.register_user`/`request_password_reset`/`confirm_password_reset`; `selectors.get_user_by_email`/`get_user_by_phone`/`get_user_by_email_or_phone`; admin registration. API surface is deliberately partial: `POST /api/v1/auth/password-reset/request/` and `.../confirm/` are live (accounts-owned, no membership check needed); `login`/`refresh`/`logout`/`me` are **not** built here — docs/authentication.md §3 requires an active `InstitutionMembership` check that `permissions` (not yet built) owns, and there is no self-serve registration endpoint in the fixed contract (docs/api-design.md §7) — `register_user` is exposed for other apps/flows to call. Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (69/69 across `core`+`institutions`+`accounts`, including live-Postgres+Redis to exercise the real anon-throttle path, not sqlite), `lint-imports` (existing core↔institutions↔accounts layer contract still kept), `manage.py spectacular` (0 warnings/errors), and live requests confirming the full request→confirm password-reset flow, the generic-ack anti-enumeration response, and the uniform `{"error": {...}}` envelope on validation failure.
- [x] `permissions` — `Role` (global template when `institution=None`, custom when set; partial-unique name index per scope), `Permission` (`scope=platform|institution`), `RolePermission`/`MembershipRole` M2M-throughs, `InstitutionMembership` (`status`, `is_default`); `selectors.is_institution_member`/`get_user_roles`/`get_membership_access` (the last one is the single Redis-cached `perm:{user_id}:{institution_id}` bundle docs/permissions.md §6 describes — 5-minute TTL, covers both roles and permission codes in one lookup); `services.create_membership`/`assign_role`/`revoke_role` (both explicitly invalidate the cache, not left to TTL expiry) /`grant_permission_to_role` (enforces docs/permissions.md §1: platform-scoped permissions can never land on an institution-defined custom role); `IsInstitutionMember`/`HasRole(*names)`/`HasPermission(code)` DRF permission classes reading `request.institution` (set by `TenantMiddleware`, never client-supplied); data migration seeding the 12 portal-default `Role(is_system=True, institution=None)` rows (System Administrator deliberately excluded — docs/permissions.md §7 grants that via `is_platform_staff`, not a `Role`, which is why the doc's own list of 13 names nets to "12 seeded"). No API surface yet (no endpoint in the fixed docs/api-design.md §7 contract calls for one at this phase) and no enforcement of docs/permissions.md §3's object-scope layer (Teacher-scoped-to-own-classes etc.) — that's each owning app's job when it's built. Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB, including the seed data migration), `pytest` (102/102 across `core`+`institutions`+`accounts`+`permissions`, live-Postgres+Redis — the cache-bundling and explicit-invalidation-on-assign/revoke behavior specifically needs real Redis, not sqlite/locmem), `lint-imports` (extended layer contract: `permissions` depends on `core`/`institutions`/`accounts`, never the reverse), `manage.py spectacular` (0 warnings/errors), and a live shell check confirming exactly the 12 expected global roles exist post-migrate.
- [x] `audit` — append-only `AuditLog` (`institution`/`actor` nullable FKs with `on_delete=SET_NULL` so a row outlives its parent, generic `target` FK using `UUIDField` for `target_object_id` since every model here has a UUID PK, `diff` JSONField, `ip_address`, `created_at` only — no `updated_at`, no soft delete); `services.log_action(actor, institution, action, target, diff, ip_address)` is the sole write path. Append-only is enforced at three levels, not just documented as a convention: `AuditLog.save()` raises on any update to an already-persisted row (`self._state.adding` check, no extra query), `AuditLog.delete()` always raises, and a custom `AuditLogQuerySet` overrides `.update()`/`.delete()` so bulk queryset operations can't bypass the instance-level guards either. Admin is registered read/list-only (`has_add_permission`/`has_change_permission`/`has_delete_permission` all `False`) rather than relying on `readonly_fields` alone, since the model-level guards would otherwise surface as an unhandled 500 on an admin edit attempt. Signal wiring on sensitive models (finance transactions, grade changes) is **not** done — those apps don't exist yet (Phase 2+). Role-assignment signal wiring was considered (`permissions.MembershipRole` exists now and modules.md's own example calls out "role assignments"), but deliberately deferred: docs/modules.md fixes `audit`'s dependencies as `core`/`institutions`/`accounts` only, not `permissions` — wiring a signal now would mean either an undocumented `audit → permissions` import or `permissions.services` calling `log_action` ad hoc, which docs/modules.md explicitly rules out ("not called ad hoc from a random call site"). Revisit once either the dependency list is deliberately extended or a Layer 1 app with an actual audit requirement lands. Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (114/114 across `core`+`institutions`+`accounts`+`permissions`+`audit`, live-Postgres+Redis), `lint-imports` (extended layer contract: `permissions` and `audit` are independent siblings, both depending on `core`/`institutions`/`accounts`), `manage.py spectacular` (0 warnings/errors), and a live shell check confirming `log_action` resolves a generic target correctly, a mutation attempt raises, and deleting the parent `Institution` nulls the FK (`SET_NULL`) rather than cascading the audit row away.
- [x] `notifications_core` — `NotificationTemplate` (`institution=None` platform default, overridable per institution — same global/override shape as `permissions.Role`, resolved by `selectors.get_template`), `NotificationLog` (`status` transitions `queued → sent|failed`, so unlike `audit.AuditLog` this one is mutable — `TimeStampedModel`, not append-only); `services.send(institution, recipient, template_key, context, channel)` only ever creates a `QUEUED` log row and calls `tasks.dispatch_notification.delay(...)` — never sends synchronously in a request; `dispatch_notification` renders the resolved template via `string.Template.safe_substitute` (deliberately not `str.format` — a context dict is effectively user-influenced input, and `str.format` templates can reach attribute/method access through a crafted key, `Template` cannot) and calls the resolved channel backend, turning any exception into a persisted `status=FAILED` row rather than a crashed/retried task. Pluggable channel backends: `NotificationChannelBackend` ABC + `NOTIFICATION_CHANNEL_BACKENDS` settings-driven resolver (mirrors the `AIProvider` ABC + settings-toggle pattern docs/roadmap.md Phase 9 describes for `ai_gateway`), defaulting every channel to `ConsoleChannelBackend` (logs instead of calling a real provider — the same role Django's own console email backend plays for local dev). Real provider backends (Africa's Talking, SES/SMTP, push) are explicitly Phase 5, not built now. `recipient` accepts a raw address string or a duck-typed user-like object (`.email`/`.phone` read via `getattr`, never an `accounts.User` isinstance check) — this app has no runtime dependency on `accounts` at all (docs/modules.md fixes its dependencies as `core`, `institutions` only), enforced by a dedicated `forbidden`-type import-linter contract, not just the layers one (with one explicit `ignore_imports` allow-list for the one test that constructs a real `accounts.User` row to exercise `NotificationLog.recipient_user`'s FK-assignment path, which genuinely requires a real model instance — Django validates FK assignment via `isinstance`, so a duck-typed stand-in can't satisfy it; the app's actual production code has zero accounts imports).

  **Two infrastructure gaps surfaced and fixed while building this app** (both pre-existing, neither introduced by this app's own code, both would have eventually bitten whichever app first genuinely exercised this machinery):
  1. `backend/config/__init__.py` was empty, so `config/celery.py`'s properly-configured Celery app was never imported at Django startup — every `@shared_task` was silently binding to Celery's unconfigured default app instead of the real Redis-backed one. Nothing before this app ever actually called `.delay()`, so it went unnoticed. Fixed with the standard Celery+Django wiring (`from .celery import app as celery_app` in `config/__init__.py`); verified live — a real message now lands in the `celery` Redis queue on `send()`.
  2. `backend/.import_linter_cache/` was untracked but **not gitignored**, and a stale copy on this machine was silently short-circuiting every `lint-imports` run this session to a cached "KEPT" result regardless of actual current source — a fresh, uncached run turned up a real (if harmless) finding: `apps/core/context.py`'s `TYPE_CHECKING`-guarded `Institution` import was being counted as a live `core → institutions` dependency and failing the very first Layer 0 layer contract, undetected because the cache never invalidated on that unchanged file. Fixed properly (`exclude_type_checking_imports = True` in `.importlinter`, since a `TYPE_CHECKING` import never executes and was never a real architectural violation) rather than by avoiding the idiom; also added `.import_linter_cache/` to `.gitignore` so this can't recur. Re-ran `lint-imports` fully fresh across the whole `apps/` tree afterward to confirm every existing contract (not just this app's new ones) genuinely passes now, not just from cache.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (136/136 across every Layer 0 app so far, live-Postgres+Redis), `lint-imports` fully fresh with no cache (both the extended layers contract and the dedicated no-accounts contract), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: `send()` → real message queued in Redis → `dispatch_notification` renders the template and transitions the log to `sent` with the console backend's response recorded.

### Cross-cutting wiring that rides along with the apps above

- [x] `AUTH_USER_MODEL = "accounts.User"` set **before the first migration runs** — landed with `accounts` itself, before any migration ran
- [x] `TenantMiddleware` (request-local, not thread-local) added to `MIDDLEWARE` — landed with `institutions`
- [x] `CorrelationIdMiddleware` added to `MIDDLEWARE` — landed with `core`
- [x] `TenantDBRouter` wired for the dedicated-DB isolation tier — landed with `core`, tested in `apps/core/tests/test_db_router.py`
- [ ] `TenantQuerySet`/manager mixin auto-filters by bound tenant on every Layer 1+ model going forward — the mixin itself exists and is tested (`TenantScopedSoftDeleteModelTests`), but nothing checks this box until a real Layer 1 model actually uses it (Phase 2)
- [x] JWT flow verified end-to-end: login, refresh, rotation, blacklist-after-rotation — `apps/permissions/views.py` (`LoginView`/`RefreshView`/`LogoutView`/`MeView`) + `serializers.py`. Lives in `permissions`, not `accounts`: docs/authentication.md §3's `InstitutionMembership` check needs a dependency `accounts` doesn't have, and `permissions` already depends on `accounts` (the reverse would be circular). `POST /api/v1/auth/login/` validates credentials (`accounts.selectors.get_user_by_email_or_phone` + `check_password`) then checks active membership at `request.institution` (set by `TenantMiddleware`, never client-supplied) — a valid password with no membership at *this* hostname fails login here, not later at the first protected endpoint, exactly as specified. Refresh token is never in a JSON body, only an httpOnly/`Secure`/`SameSite=Strict` cookie (`Secure` off in `dev.py` only, since local dev is plain `http://`) with no `domain` ever set (scopes it to the exact requesting hostname, never a wildcard) and `path=/api/v1/auth/`. `RefreshView` reuses SimpleJWT's own `TokenRefreshSerializer` for the actual rotation/blacklist-after-rotation logic (`ROTATE_REFRESH_TOKENS`/`BLACKLIST_AFTER_ROTATION`, already configured) rather than reimplementing it, re-raising its `TokenError` as a plain `AuthenticationFailed` (SimpleJWT's own `InvalidToken` has a nested-dict `detail` that `api.exception_handlers`'s single-`detail`-key check would stringify as a raw dict repr). `GET /api/v1/auth/me/` returns the user plus `institution_membership` (`null`, not an error, when a session outlives a mid-window-revoked membership — docs/permissions.md §6). Verified via ruff, `pytest` (151/151 across every Layer 0 app, live-Postgres+Redis, including cookie-rotation and old-token-blacklisted-after-rotation assertions), `lint-imports`, `manage.py spectacular` (0 warnings/errors), and a live curl run against a real `Domain`-bound hostname: login → me (roles correct) → refresh (rotated access token) → logout (cookie cleared) → refresh again correctly rejected (hit the live anon rate limiter, confirming that's live too).
- [x] Health-check endpoint for Nginx/uptime monitoring — `/healthz/`, landed with `core`, verified live
- [x] Dashboard shell — empty per-role portal pages wired into `router.tsx`, no data yet — 11 of the 12 portal folders (`institution-admin`, `principal`, `finance-officer`, `teacher`, `parent`, `student`, `librarian`, `nurse`, `receptionist`, `transport-manager`, `hostel-warden`) are wired as guarded, statically-written `lazy: () => import(...)` routes, each rendering a shared `PortalDashboardPlaceholder`. `system-admin`'s folder exists for structural completeness only and is deliberately **not** wired into the router — that access model is `is_platform_staff`, not a role, and there's no `InstitutionMembership` for `RequireRole` to check yet (docs/permissions.md §7's break-glass flow isn't built). Verified via `npm run build`: exactly 11 separate `routes-*.js` chunks in `dist/`, confirming Vite is actually code-splitting per portal, not just per-route in name only (docs/frontend-architecture.md §1).
- [x] Frontend: login screen, password reset screen, route guards per role — `app/pages/LoginPage.tsx`/`ForgotPasswordPage.tsx`/`ResetPasswordPage.tsx` compose presentational forms from `features/auth` (Zod schemas + React Hook Form, docs/frontend-architecture.md §4); `app/providers/AuthProvider.tsx` silently exchanges the refresh cookie for an access token on boot before rendering any protected route, then loads `/me/`; `app/RequireAuth.tsx`/`RequireRole.tsx` are the client-side-only UX gates (never the authorization boundary — the API enforces this regardless, docs/frontend-architecture.md §1) and also supply the one shared header (user email + sign-out) every authenticated route gets. `app/pages/PostLoginRedirect.tsx` lands a single-role user directly in their portal, or shows a lightweight picker for multiple roles (e.g. Deputy Principal who also teaches) via `app/portal-routing.ts`'s role→portal-slug map — de-duplicated, since Principal and Deputy Principal resolve to the same portal. A real architectural boundary shaped this: `features/*` and `portals/*` may not import from `app/` (ESLint `eslint-plugin-boundaries`), so `AuthProvider`/`useAuth` had to live in `app/` per docs/project-structure.md while the actual auth *pages* also moved to `app/pages/` (not `features/auth/pages/` as first drafted) so they could compose `useAuth()` — and the shared header takes `onLogout`/`userLabel` as plain props rather than reading context itself, keeping it in `shared/components` where it belongs. Two small infra gaps fixed along the way, neither specific to this feature: (1) `ApiError` was defined inside `shared/lib/api.ts`, which `auth.ts` needed too, but `api.ts` already imports from `auth.ts` — extracted to `shared/lib/api-error.ts` to avoid a circular import; (2) RTL's auto-cleanup-between-tests relies on detecting a global `afterEach`, which Vitest doesn't provide unless `test.globals` is enabled — added explicit `cleanup()` in `src/test-setup.ts` instead of enabling globals, after the first component test's second `it()` block found three stacked forms in the DOM from the previous one. Verified via `npm run typecheck` (0 errors), `npm run lint` (0 errors — including the boundaries rule, which is what caught the `app`-import violation during development), `npm run test` (12/12: `LoginForm` interaction tests, `resetPasswordSchema` mismatch/min-length tests, `portalSlugsForRoles` de-dup tests), `npm run build` (clean, confirms per-portal code-splitting), and a full live browser run against a real backend (temporary `vite.config.ts` proxy override to reach a local ephemeral Postgres/Redis + a real `Domain`-bound hostname, reverted immediately after): login with client-side Zod validation errors on empty submit → successful login → auto-redirect to `/teacher` (the one seeded role) → guarded route renders the lazy-loaded portal chunk → header shows the signed-in user → sign-out returns to `/login`. No console errors (only React Router's benign v7 future-flag warning). Also added `@testing-library/user-event` as a devDependency (referenced by `package.json`/`jest-dom` but not actually installed) and a `test:` block + `src/test-setup.ts` to `vite.config.ts` (neither existed — `npm run test` only "passed" before because there were zero test files to run).

### Milestone

- [ ] A fully operational SaaS platform capable of hosting multiple schools securely

---

## Phase 2 — Core Academic Engine

- [x] `classes_streams` — `AcademicYear`, `Term` (`is_current`, partial-unique "at most one current term per institution"), `ClassGrade` (real intra-app FK to `Term`; `curriculum_type` reuses `institutions.InstitutionCurriculum.CurriculumType`), `Stream`, `ClassTeacherAssignment` (`staff_id` a plain cross-app UUID to `staff.StaffProfile`, which doesn't exist yet; partial-unique constraints correctly allow a class-level assignment and a stream-level assignment on the same class to coexist). `selectors.get_current_term(institution)`/`get_class_grade(...)` bind `institution` explicitly via `bind_institution` rather than relying on ambient context, so they work both inside a request and from a context with nothing bound at all (a future Celery task, docs/authentication.md §7) — verified with a dedicated test asserting this explicitly. Full DRF API (`/api/v1/academic-years/`, `/terms/` + a `set-current` RPC action, `/class-grades/`, `/streams/`, `/class-teacher-assignments/`): reads open to any active member, writes gated by a `classes_streams.{resource}.manage` permission code — first real Layer 1 API surface built this phase, unlike most Phase 1 Layer 0 apps.

  **Real, foundational issues surfaced by being the first app to actually use `TenantScopedModel`/`ModelViewSet`/`ModelSerializer` together** (all fixed in `core`/`api`, not worked around locally, since every one of the 7 remaining Phase 2 apps would hit the same thing):
  1. `TenantScopedModel` extended `UUIDPrimaryKeyModel` directly, with no `created_at`/`updated_at` — but docs/database.md §1 says timestamps are "not optional per-model." Never surfaced before because no app had used `TenantScopedModel` on a real model until now. Fixed: it now extends `TimeStampedModel` instead (still single inheritance, no manager-collision risk).
  2. DRF's class-level `queryset = Model.objects.all()` convention is incompatible with `TenantManager`, which raises `TenantContextMissing` *eagerly* the moment `.all()`/`.filter()` is called (verified/intentional per `core`'s own tests) — a class-body attribute evaluates at import time, before any request (and its bound tenant) exists, so it broke Django startup entirely. Fixed with a shared `api.viewsets.TenantScopedModelViewSet` — subclasses set `queryset_model`, and `get_queryset()` resolves it fresh per request instead, the standard DRF pattern for any per-request-dynamic queryset.
  3. `manage.py spectacular` schema generation (no real request, so no bound tenant) crashed twice more: DRF's `ModelSerializer` builds uniqueness validators for conditional `UniqueConstraint`s by calling `model._default_manager.filter(...)` directly — bypassing any view-level fix entirely. Fixed with a shared `api.serializers.TenantScopedModelSerializer` that skips validator generation when no tenant is bound (real requests always have one, so real validation is unaffected). Separately, `TenantManager`/`TenantScopedSoftDeleteManager`/`TenantScopedAllManager` didn't have a working `.none()` — third-party schema-introspection tooling calls `Model._default_manager.none()` directly, and `.none()` never touches real data anyway (Django's `EmptyQuerySet` short-circuits before hitting the DB), so it's now exempted from the tenant-bound check everywhere, with a regression test in `core`.
  4. django-filter's automatic `filterset_fields` goes one layer deeper still — `resolve_field()` calls `model_field.model._default_manager.all().query` purely to introspect a *related* field's type, still outside any bound tenant. Rather than reshaping `core`'s query-execution model further under time pressure (a bigger, higher-stakes change to a security-critical mechanism, deserving its own dedicated pass, not a rushed mid-feature fix), `filterset_fields` is deliberately not used here — an explicit `django_filters.FilterSet` with hand-declared filters (not auto-derived from `Meta.fields`) avoids the same crash and is the correct way to add filtering later, noted inline in `views.py`.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (185/185 across every app so far, live-Postgres+Redis), `lint-imports` fully fresh (new Layer 1 layer contract + an `independence` contract for Layer 1 siblings, ready to extend as each subsequent Phase 2 app lands), `manage.py spectacular` (0 warnings/errors — only reached after fixes 3 and 4 above), and a live end-to-end run: login → create `AcademicYear` → create `Term` → `set-current` action → list confirms `is_current` → `selectors.get_current_term` independently confirms the same row.
- [x] `students` — `Student`/`Enrollment` use `TenantScopedSoftDeleteModel` (both named in docs/database.md §1's soft-deletable list); `GuardianRelationship` is plain `TenantScopedModel` (hard-delete, not on that list) and lives here rather than in `parents`, since a guardian may have no `ParentProfile`/portal account at all (e.g. an emergency contact) — the relationship must not depend on that profile existing. `Enrollment.class_grade_id`/`stream_id`/`term_id` are plain cross-app UUIDs into `classes_streams`, a *sibling* Layer 1 app `students` has no sanctioned import of (only `parents`→`students` and `admissions`→`students` are documented edges) — same reasoning as the Layer 0 cross-app convention, just enforced here for a second, non-cross-database reason too (the `layer-1-siblings-independent` `independence` contract). `selectors.get_active_roster`/`get_guardian_children`/`get_active_enrollment`/`get_student_by_user_id`; `services.create_student`/`enroll_student` (the one sanctioned `admissions`→`students` write chain, per docs/modules.md) /`add_guardian`. Full DRF API for all three models — `StudentViewSet.get_queryset()` layers docs/permissions.md §3's object-scope restriction on top of the permission check: a Parent is scoped to `get_guardian_children(...)` and a Student to their own record via `user_id`, **regardless of what broader permission they might also hold** (fail-closed by design, not just "whichever check runs first") — verified with a dedicated test granting a Parent the broad `students.student.manage` permission and confirming they still only see their own child, and again live.

  Went noticeably faster than `classes_streams` — the four foundational `TenantScopedModel`/DRF/spectacular fixes made there carried over cleanly with zero new framework-level surprises. One small, genuine viewset-layer refactor: `api.viewsets.TenantScopedModelViewSet` gained a `get_base_queryset()` method (the `swagger_fake_view` handling, factored out of `get_queryset()`) so `StudentViewSet` could layer its object-scope `.filter()` on top without duplicating that handling — `classes_streams`'s viewsets are unaffected (their `get_queryset()` still just returns `get_base_queryset()`).

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (211/211 across every app so far, live-Postgres+Redis), `lint-imports` fully fresh (`students` added to both the Layer 1 `layers` and `independence` contracts), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: a Parent user with two children in the DB (only one their own) logs in and lists `/api/v1/students/`, receiving exactly their own child — confirming the object-scope filter works over real HTTP, not just against a Django test client.
- [x] `staff` — `StaffProfile` (`TenantScopedSoftDeleteModel`, named in docs/database.md §1's soft-deletable list as "Staff"; `user_id` plain UUID like every cross-app reference to `accounts.User`, but **not nullable** — unlike `Student.user`, every staff member has a platform login per docs/database.md §3's `user[1:1]`; `first_name`/`last_name` added even though not in that field list, since `accounts.User` is identity-only with no name fields and this is the same gap `students.Student` already had). `department` is a plain field on the model itself, not a separate `Department` entity — matches docs/database.md §3's field list exactly, and a full `Department` model isn't asked for anywhere in Phase 2. `services.create_staff_profile`; `selectors.get_staff_by_user_id`/`get_staff_by_department`. Full DRF API at `/api/v1/staff/`, permission-gated only — no object-scope layer, unlike `students`: docs/permissions.md doesn't call out a "staff can only see their own profile" restriction the way it does for Parent/Student, so none was invented.

  **Deliberately not built**: `selectors.get_teachers_for_subject(...)` (docs/modules.md) and the Teacher-role object-scope selector `get_assigned_classes(staff_user)` (docs/permissions.md §3, described as living in `staff`). Both need data this app has no sanctioned import of — subject specialization belongs to `academics.SubjectCatalog` (not built until later this phase) and actual class/subject assignments belong to `timetable.SubjectSlotAssignment` (built after `staff`, per this checklist's own fixed order) — building either now would mean guessing at a shape neither dependency has fixed yet. Add them once `academics`/`timetable` exist to call into; this is the same "defer until the real dependency exists" call already made for `academics`'s resolver stub and `audit`'s signal wiring.

  Went as fast as `students` — no new framework-level surprises, all four `classes_streams`-era fixes continue to carry over cleanly. Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (224/224 across every app so far, live-Postgres+Redis), `lint-imports` fully fresh (`staff` added to both the Layer 1 `layers` and `independence` contracts), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: create a `StaffProfile` via the API, list confirms it.
- [x] `parents` — `ParentProfile` (plain `TenantScopedModel`, not on docs/database.md §1's soft-delete list). Deliberately owns no guardian↔child link data itself — `selectors.get_children` is a thin pass-through to `students.selectors.get_guardian_children`, the one sanctioned `parents`→`students` import (docs/modules.md). This is the first app to actually exercise a sanctioned cross-Layer-1 edge, which needed a real `.importlinter` change: the combined "Layer 1 depends on Layer 0" `layers` contract (not the separate `independence` one — `parents` has a real sibling dependency, so it can't join the "fully independent siblings" set) gained `parents` in its top tier plus an `ignore_imports` entry for exactly `parents.selectors → students.selectors`, discovering along the way that `ignore_imports` works on `type = layers` contracts too, not just `forbidden`/`independence` ones — plus a second, test-only `ignore_imports` entry for constructing real `Student`/`GuardianRelationship` rows in `parents`' own test suite (same "duck-typing can't satisfy a real FK-style lookup" category as `notifications_core`'s earlier test exception). `ParentProfileViewSet` self-scopes a Parent to their own profile only, regardless of broader permission — same fail-closed pattern as `students`/`staff`'s Student/Parent scoping.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB — caught a missing `Meta.ordering` via the pagination warning again, fixed and squashed into the one migration before it ever shipped), `pytest` (235/235 across every app so far, live-Postgres+Redis, including a selector test that constructs real cross-app data to prove the pass-through reaches it), `lint-imports` fully fresh, `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: a parent creates their own profile via the API and lists it back.
- [x] `academics` — deliberately small, per docs/checklist.md's own framing ("this is the contract, not curriculum logic — that's Phase 3"): `contracts.AssessmentEngine`/`ReportEngine` are abstract base classes with no models and no implementation, `GradingScale` (`curriculum_type` + `levels` JSONField, unique per institution+curriculum_type) and `SubjectCatalog` (unique `code` per institution+curriculum_type) are both plain `TenantScopedModel` — the generic "subject" concept `timetable`/future curriculum plugins specialize via FK rather than each inventing its own subject list. `selectors.get_curriculum_engine(institution, curriculum_type)` is a genuine stub that raises `NotImplementedError` — no curriculum plugin registry exists until Phase 3, so it doesn't pretend to resolve anything. `academics` has its own broader documented dependency set (`core`, `institutions`, `students`, `classes_streams`, `staff`) rather than joining the Layer 1 "independent siblings" `independence` contract, so it got a dedicated standalone `layers` contract instead. Full DRF API at `/api/v1/grading-scales/` and `/api/v1/subjects/` — reads open to any active member, writes gated by `academics.grading_scale.manage`/`academics.subject_catalog.manage`.

  Two bugs surfaced and fixed here, both worth carrying forward: (1) a test-only bug — DRF's test client defaults to `multipart` encoding, which can't represent a nested JSON structure (`GradingScale.levels`, a list of dicts) and silently produced a 400; fixed by passing `format="json"` explicitly on that one POST (`SubjectCatalog`'s test didn't hit this since all its fields are flat strings). (2) a real, systemic test-isolation bug, not just an `academics`-local one: `CACHES["default"]` is Redis-backed and used for DRF's anon-throttle counters, but unlike the DB, Redis isn't wrapped in each test's transaction rollback — so anon-throttle hit counts accumulate across the *entire* pytest session. As the full suite grew, it started exceeding the 5/min anon bucket by the time later-alphabetical test files (`accounts`, `permissions`) ran, even though those specific test classes carry an `@override_settings(REST_FRAMEWORK=_THROTTLING_DISABLED)` decorator — which turned out to never have worked as intended, since `APIView.throttle_classes = api_settings.DEFAULT_THROTTLE_CLASSES` is resolved once at class-definition time (when `rest_framework.views` is first imported), not re-read per-request, so overriding the `REST_FRAMEWORK` setting later doesn't actually change an already-defined view's throttle classes. Fixed at the root with a new `backend/conftest.py` autouse fixture that clears the cache before every test — this is a general fix for the whole suite going forward (`admissions` is about to add another public, aggressively-throttled endpoint, which would have made this worse, not better).

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (252/252 across every app so far, live-Postgres+Redis), `lint-imports` fully fresh (new dedicated `academics-dependencies` layers contract, 5 contracts total, all kept), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: login → create a `SubjectCatalog` row → create a `GradingScale` row → list each back via the API.
- [x] `timetable` — `Timetable` (`term_id`/`class_grade_id`, both plain cross-app UUIDs to `classes_streams`, unique per institution+term+class_grade), `Period` (real intra-app FK to `Timetable`; `day_of_week` `IntegerChoices` 0=Monday..6=Sunday; unique per timetable+day+start_time; `CheckConstraint` enforcing `start_time < end_time`), `SubjectSlotAssignment` (real intra-app FK to `Period`; `subject_id`/`staff_id` plain cross-app UUIDs to `academics.SubjectCatalog`/`staff.StaffProfile`; `room` a plain `CharField`, no separate `Room` model — not asked for anywhere in Phase 2; unique per period, enforced at the DB level even though `services.assign_slot`'s own `update_or_create` already guarantees it). No real Python import of `classes_streams`/`academics`/`staff` anywhere in this app — every cross-app reference is a plain UUID field, never a live FK — so unlike `academics`, `timetable` didn't need its own dependency-specific `.importlinter` contract: it joined the existing flat "Layer 1 depends on Layer 0" `layers` contract's top tier and the `layer-1-siblings-independent` `independence` contract, same as `classes_streams`/`students`/`staff` originally did.

  `selectors.get_timetable`/`get_periods`/`get_staff_schedule` (the last one is the exact query shape `assign_slot`'s clash-detection uses, exposed as a selector too so `staff`'s still-unbuilt "assigned classes" selector won't have to duplicate it later). `services.create_timetable`/`create_period`/`assign_slot` — `assign_slot` is the one function worth calling out: before writing, it checks every other `SubjectSlotAssignment` for the same `staff_id` where `period.day_of_week` matches and the time ranges overlap (`start_time__lt=period.end_time` AND `end_time__gt=period.start_time`), *excluding the period being assigned* — deliberately not scoped to the same `Timetable`, since the clash is about the staff member being double-booked across the whole institution, not just within one class's schedule. This is real cross-row, cross-table logic no `UniqueConstraint` could express, exactly as docs/modules.md calls out. `update_or_create(period=...)` means re-assigning an already-assigned period updates it in place rather than duplicating — verified with a dedicated test, same pattern as `classes_streams.assign_class_teacher`.

  API: `/api/v1/timetables/`, `/api/v1/subject-slot-assignments/` (flat), and `/api/v1/timetables/<id>/periods/` — the one nested route in the API so far, docs/api-design.md §1's explicitly-named example ("a sub-resource with no independent identity outside its parent"), hand-rolled with two `path()` entries calling `PeriodViewSet.as_view({...})` directly rather than adding a `drf-nested-routers` dependency for a single nested resource. `PeriodViewSet.get_queryset()` filters by `self.kwargs["timetable_pk"]` (guarded for `swagger_fake_view`, where `self.kwargs` is empty during schema generation — confirmed by reading `drf_spectacular.generators.create_view`, which instantiates views with `kwargs = {}`) and `perform_create` injects `timetable_id` from the URL, never the request body. `SubjectSlotAssignmentViewSet.create`/`update` both route through `assign_slot` rather than the generic save path, since clash-detection has to run on every write, not just the first one.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (276/276 across every app so far, live-Postgres+Redis, including dedicated clash-detection tests: overlapping periods on different timetables are rejected, non-overlapping periods and different-day/different-staff combinations are allowed), `lint-imports` fully fresh (5 contracts, all kept — `timetable` added to the existing flat Layer 1 layers/independence contracts, no new contract needed), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: login → create a `Timetable` → create two overlapping `Period`s on it via the nested route → assign one staff member to both → the second assignment is rejected over real HTTP with a `400` and the exact clash message, not just in the Django test client.
- [x] `attendance` — `AttendanceRecord` (`term_id`/`target_id` plain cross-app UUIDs — `target_id` is `students.Student` or `staff.StaffProfile` depending on `subject_type`; unique per institution+term+date+subject_type+target_id, confirmed a same-UUID collision across a Student and a StaffProfile still wouldn't collide since `subject_type` is part of the key). No policy config model — nothing in docs/database.md §3 or docs/modules.md calls for one beyond the record itself, so none was invented (same "don't guess a shape nothing has fixed yet" call made for `staff`'s deferred selectors and `academics`'s resolver stub).

  `selectors.get_records`/`get_attendance_rate`/`get_records_for_date`. `get_attendance_rate` counts `present` and `late` as "attended" (late still means physically present) against the total, and returns `None` — not `0.0` — when there are no records at all, so a caller can tell "perfect attendance" apart from "no data yet" (verified with a dedicated test). `services.mark_attendance` is `update_or_create`-keyed on the unique constraint — marking the same target on the same day twice corrects the record rather than duplicating it, same idempotent-write pattern used by `classes_streams.assign_class_teacher` and `timetable.assign_slot`.

  Full DRF API at `/api/v1/attendance/`, with the first genuinely hand-declared `django_filters.FilterSet` in the project (`AttendanceRecordFilterSet`: `date`/`subject_type`/`target_id`, docs/api-design.md §6's explicit-whitelist requirement) — confirmed safe against the `filterset_fields`-auto-derivation crash every earlier app avoided (`classes_streams`'s views.py comment), by reading django-filter's `get_filters()`/`get_model_field()` source directly: a field listed in `Meta.fields` that already has a matching hand-declared filter skips the crash-prone `filter_for_field`/`resolve_field` path entirely, so `Meta.fields = ["date", "subject_type", "target_id"]` alongside three explicit `Filter` instances is safe. `AttendanceRecordViewSet.create`/`update` both route through `mark_attendance`, same "generic save path can't run this app's real invariant" reasoning as `timetable`'s `SubjectSlotAssignmentViewSet`.

  Deliberately not built: the Teacher-scoped-to-own-classes object-scope filter docs/permissions.md §3 describes (a Teacher should only mark attendance for classes they're assigned to teach). It needs `staff.selectors.get_assigned_classes(staff_user)`, which was deliberately deferred when `staff` was built pending `timetable` — `timetable` exists now, but wiring `get_assigned_classes` would mean giving `staff` a new real cross-app import into `timetable` (querying `SubjectSlotAssignment` by `staff_id`), which isn't free: it would pull `staff` out of the flat Layer-1-siblings `independence` contract it's currently in, same tradeoff `parents`/`academics` already made deliberately, not accidentally. Given time pressure to land all four remaining Phase 2 apps in one pass, this is deferred with the same reasoning as `staff`'s original deferral: add it once there's a concrete caller ready to use it, rather than guessing the shape now. `attendance` writes are permission-gated only for now, same as `staff`'s own precedent (no object-scope layer invented where the docs don't strictly force one for Phase 2 to be functionally complete).

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (291/291 across every app so far, live-Postgres+Redis), `lint-imports` fully fresh (5 contracts, all kept — `attendance` added to the existing flat Layer 1 layers/independence contracts, no new contract needed, no real cross-app import anywhere in this app), `manage.py spectacular` (0 warnings/errors — including the new hand-declared `FilterSet`), and a live end-to-end run: login → mark a student present → remark the same student on the same day absent (confirmed updated in place, not duplicated) → mark a second day present → `?date=` filter confirms exactly one record for the filtered day over real HTTP.
- [x] `admissions` — `Application` (`applicant_details` an unstructured `JSONField`, same "too institution-specific for a fixed schema" call as `academics.GradingScale.levels`; `stage` a `TextChoices` with a `CheckConstraint`; `term_applying_for_id` a plain cross-app UUID to `classes_streams.Term`), `ApplicationStage` (append-only stage-transition history — `Application.stage` stays the fast-to-filter current value, `ApplicationStage` rows are the timestamped audit trail of how it got there, same "current value + history table" split `audit.AuditLog` already established), `Offer` (plain FK to `Application`, not `OneToOne` — docs/database.md §3's `└─<` notation is one-to-many, and re-offers after a decline are a real scenario worth allowing at the schema level even though nothing exercises it yet).

  This is the last Phase 2 app and the second (and final) genuinely cross-app Layer 1 write chain: `services.convert_to_enrollment` calls `students.services.create_student`/`enroll_student` directly — the one place any Layer 1 sibling other than `parents` is allowed to touch `students`' state (`parents` only *reads* guardian links; this *writes*). `.importlinter`'s combined Layer 1 `layers` contract gained `admissions` in its top tier (same tier as `parents`, not the `independence` set) plus `ignore_imports` entries for `apps.admissions.services -> apps.students.models`/`apps.students.services`, and a test-only entry for constructing real `Student`/`Enrollment` rows to confirm the chain actually wrote real data. `convert_to_enrollment` refuses to run unless `application.stage == ACCEPTED` (verified with a dedicated test) — conversion is meant to follow a real accepted offer, not be callable from any stage.

  `services.submit_application` (creates the `Application` + a `SUBMITTED` history row) is the public entry point; `make_offer`/`accept_offer` each advance `Application.stage` and append a history row inside `transaction.atomic`, same pairing-of-writes discipline `classes_streams.set_current_term` established. `selectors.get_application`/`get_applications_by_stage`.

  API: `POST /api/v1/applications/` is genuinely public — `AllowAny`, no institution membership required, since a prospective parent has no account yet (docs/authentication.md §6) — still tenant-resolved via the `Host` header, since `TenantMiddleware` runs before authentication; relies on the project-wide default `AnonRateThrottle` rather than a bespoke rate, same pattern as `accounts`'s password-reset endpoints, verified live with zero `Authorization` header at all. List/retrieve and the three lifecycle actions (`make-offer`, `accept-offer`, `convert-to-enrollment` — RPC-style per docs/api-design.md §1, not an overloaded `PATCH`) require institution membership, and the three actions additionally require `admissions.application.manage`. `ApplicationSerializer` nests read-only `stage_history`/`offers` so a staff member can see an application's full timeline in one request rather than needing three separate endpoints — deliberately no standalone `Offer`/`ApplicationStage` list endpoints, since neither has an access pattern independent of its parent `Application` the way docs/api-design.md §1 reserves nesting/embedding for.

  While writing this app's tests, confirmed something learned during `academics`: the `@override_settings(REST_FRAMEWORK=_THROTTLING_DISABLED)` pattern copied into every earlier throttle-adjacent test file never actually disabled throttling (`APIView.throttle_classes` is resolved once at import time, not re-read per-request) — it "worked" only because request volume never got close to the anon cap. This app's tests skip that pattern entirely and rely purely on `backend/conftest.py`'s cache-clearing fixture, which is now confirmed to be the actual, only mechanism providing real test-to-test throttle isolation.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (309/309 — every Phase 2 app complete, live-Postgres+Redis), `lint-imports` fully fresh (5 contracts, all kept), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: an anonymous request with no `Authorization` header submits an application → an authenticated staff member with `admissions.application.manage` makes an offer → accepts it → converts to enrollment, producing a real `students.Enrollment` row → retrieving the application afterward shows `stage=enrolled` and the full four-row stage history over real HTTP. Ephemeral verification containers (`educore-postgres-hostport`/`educore-redis-hostport`, ports 5433/6379) and all smoke-test institutions cleaned up after this app — Phase 2 backend is complete.

### Milestone

- [ ] Schools can fully manage their academic structure before curriculum-specific features are introduced

---

## Phase 3 — Curriculum Plugin Framework

**Scoped to the framework plus one reference plugin at a time**, not all five in
one pass — confirmed with the user up front given the full phase is roughly
Phase-2-sized or larger per plugin. Framework + CBC landed first; `curriculum_844`
confirmed the framework generalizes to a numeric-score/letter-grade curriculum;
`curriculum_british` added a predicted grade scoped to academic year rather than
term; `curriculum_tvet` is the fourth, the most hierarchical plugin so far
(Department → Course → CompetencyUnit) and the first whose top-level entity
isn't documented as specializing `academics.SubjectCatalog`. `curriculum_university`
and the Plugin SDK's testing-framework/version-compatibility tooling remain
explicit follow-ups (no external plugin developers exist yet to build tooling for).

- [x] Curriculum registry + plugin loader — `apps/academics/registry.py`: a plain `dict[str, type]` with `register(curriculum_type, engine_class)`/`resolve(curriculum_type)`. Each curriculum app self-registers via its own `AppConfig.ready()` (`curriculum_cbc/apps.py` is the reference) rather than `academics` importing curriculum apps itself — docs/modules.md's Layer 2 inversion is otherwise unenforceable: `academics` genuinely cannot contain a static `curriculum_cbc` import anywhere, even inside the resolver, or it would itself be the forbidden reverse dependency. `resolve()` instantiating an incomplete `AssessmentEngine`/`ReportEngine` subclass raises `TypeError` via Python's own ABC machinery — free "plugin validation," no separate check needed. `contracts.py`'s two ABCs gained concrete signatures (`institution`/`student_id`/`term_id`/`details`, previously `*args, **kwargs` since nothing implemented them yet).
- [x] `academics.selectors.get_curriculum_engine(institution, curriculum_type=None)` fully resolves — looks up `institutions.InstitutionCurriculum`'s active rows for the institution (an institution may run several curricula concurrently, docs/database.md §2), defaults to the one active curriculum when unambiguous, raises `ValueError` when the caller must disambiguate or asks for a curriculum the institution doesn't run, then delegates to `registry.resolve(...)`. New `get_curriculum_type_for_student(institution, student, term_id)` selector resolves which curriculum a student's *current enrollment* falls under (`students.get_active_enrollment` → `classes_streams.get_class_grade` → `.curriculum_type`, both already-sanctioned academics dependencies from Phase 2, reused as-is) — the mechanism the two new generic endpoints below use so a client never has to know or pass curriculum_type itself.
- [ ] Plugin SDK: interface (`contracts.py` — done), validation (free via ABC instantiation — done), testing framework, version compatibility — the latter two deliberately deferred, meta-tooling for third-party plugin authors that don't exist yet.
- [x] **The curriculum-agnostic generic API** (docs/api-design.md §8) — the part that actually proves the plugin abstraction is load-bearing, not just registered-and-unused: `POST /api/v1/assessments/` and `GET /api/v1/report-cards/<student_id>/<term_id>/`, both plain `APIView`s in `academics/views.py` (RPC-style facades over a resolved engine, not CRUD on a model `academics` owns) that resolve curriculum_type server-side and never expose it as a client-facing parameter. `AssessmentRecordView` is gated by `academics.assessment.record`; `ReportCardView` is object-scoped exactly like `students.StudentViewSet` (Parent must be in `get_guardian_children`, Student must own the record, fail-closed regardless of broader permissions) — everyone else with `IsInstitutionMember` may pull any student's report. Small addition to `students/selectors.py`: `get_student_by_id` (two lines, same shape as `get_student_by_user_id`), needed by curriculum plugins' own report assembly. (Note: docs/api-design.md §8's illustrative URLs show an `/academics/` prefix; every app built in Phase 1/2 uses a flat namespace, so these two routes register flat too, for consistency with 8 already-shipped apps rather than the doc's literal illustration.)
- [x] `curriculum_cbc` — `LearningArea` (specializes `academics.SubjectCatalog` via a plain cross-app UUID, per docs/database.md §3's explicit "specialize via FK" pattern), `Competency` (real intra-app FK to `LearningArea`; `strand`/`sub_strand`), `CoreValue`/`PCI` (**deliberately no seeded data** — CBC's official core-value/PCI lists are real curriculum content not confident enough to hardcode as fact; institutions enter their own), `Project`, `ContinuousAssessment` (`performance_level` — the 4 standard CBC tiers as a fixed `TextChoices` rubric, unlike core values/PCIs this is structural, not curriculum "content" — unique per institution+student+competency+term, `services.record_assessment` is `update_or_create`-keyed on it, same idempotent-write pattern as `mark_attendance`/`assign_slot`). `engine.py`'s `CBCEngine(AssessmentEngine, ReportEngine)` is a thin delegator to `services.py`/`selectors.py` — no business logic lives in the class itself, matching every other app's function-based split; `compute_result` returns the mode performance level per learning area (CBC has no single numeric grade), `generate_report_data` assembles a nested learning-area → competencies structure plus the term's `Project`s. Full CRUD API for the five reference-data/log models at `/api/v1/curriculum-cbc/...` — the one hyphenated, non-flat URL namespace in the project so far, docs/api-design.md §8's explicit convention for operations with no cross-curriculum equivalent. **Deliberately no CBC-specific assessment-recording endpoint** — that's the whole point of the generic endpoint above.
- [x] `curriculum_844` — `Subject` (same specialize-via-FK shape as `curriculum_cbc.LearningArea`), `ExamResult` (one model with an `exam_type` `TextChoices` discriminator — CAT/Midterm/EndTerm/Mock/KCPE-KCSE — rather than four near-identical tables; docs/database.md §4's parenthetical lists `exam_type` as a shared field alongside student/subject/term/score/max_score, reading as one model, not an invented simplification; `score`/`max_score` are `DecimalField`, not float — precision matters at grade boundaries; `CheckConstraint`s enforce `0 <= score <= max_score` and `max_score > 0`), `MeanGradeSnapshot` (`mean_score`/`mean_grade`/`rank_in_class`/`rank_in_stream` — **the one model in either plugin so far that's explicitly precomputed, not request-time**: docs/database.md §4 states ranking needs every student in the class recomputed together, "not computed live per request"). `services.recompute_mean_grade_snapshots` is the real logic behind `tasks.py`'s `@shared_task` (thin wrapper, same split `notifications_core.tasks.dispatch_notification` established) — mean_score is an **unweighted** mean of `(score/max_score*100)` across every `exam_type` (no documented weighting scheme between CAT/Midterm/EndTerm/Mock exists, so none was invented), `mean_grade` resolves against `academics.GradingScale('844')`'s `levels` — the first real *consumer* of `GradingScale`, which sat unused since Phase 2 since CBC's qualitative rubric never needed it. `engine.py`'s `EightFourFourEngine(AssessmentEngine, ReportEngine)` — same thin-delegator shape as `CBCEngine`; `compute_result` returns the current snapshot if one exists, otherwise falls back to a live, unranked single-student calculation (ranking stays precompute-only, per the docs). Small addition to `students/selectors.py`: `get_active_enrollments(institution, class_grade_id, term_id)` (explicit-institution-argument self-binding, like `classes_streams.get_current_term` — needed because ranking runs from a Celery task with nothing ambiently bound, and needs each student's `stream_id` for `rank_in_stream`, which a plain `Student` queryset doesn't carry).

  API: `/api/v1/curriculum-844/subjects/` (ordinary CRUD, same pattern as `curriculum_cbc`'s reference-data endpoints). **No `ExamResult` CRUD endpoint** — recording goes through the same generic `/api/v1/assessments/` endpoint CBC uses, confirming that endpoint is genuinely curriculum-agnostic, not CBC-shaped. Two 844-specific RPC endpoints with no cross-curriculum equivalent (docs/api-design.md §8): `POST /api/v1/curriculum-844/recompute-mean-grades/` (enqueues the Celery task) and `POST /api/v1/curriculum-844/kcpe-kcse-results/import/` (bulk import, one `transaction.atomic()` — the whole batch succeeds or the first invalid row rolls everything back; no partial-success/error-collection convention invented, matching every other write in this codebase). Object-scoping on report cards needed **zero new code** — confirmed live that the framework's `ReportCardView` correctly denies a Parent pulling a different student's 844 report, with no 844-specific object-scope logic anywhere in this app.

  `.importlinter` gained a `curriculum-844-dependencies` layers contract (identical shape to `curriculum-cbc-dependencies`), `apps.curriculum_844` added to `no-layer1-imports-curriculum-plugins`'s `forbidden_modules`, and a new `curriculum-plugins-independent` `independence` contract added retroactively — the per-plugin `layers` contracts alone don't check plugins against *each other* (each one's tier list simply doesn't mention sibling plugin apps), so nothing previously stopped `curriculum_844` from importing `curriculum_cbc` directly; this closes that gap explicitly rather than leaving it to accident.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (390/390 across every app, live-Postgres+Redis), `lint-imports` fully fresh (9 contracts, all kept), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run with a **real Celery worker** (`celery -A config worker -P solo`, not just enqueue-and-hope): a teacher creates a `Subject` → records two students' `ExamResult`s via the *generic* `/api/v1/assessments/` endpoint, confirming it resolves to `EightFourFourEngine`, not CBC's → `POST recompute-mean-grades/` enqueues the task, confirmed actually consumed and completed via the worker's own log (`succeeded in 0.126s`) → both students' report cards via the *generic* `/api/v1/report-cards/` endpoint show correct `mean_grade`s (90→A, 30→C, resolved from the seeded `GradingScale`) and correct `rank_in_class`/`rank_in_stream` → the top student's guardian sees the report (200), a different student's report is denied (403) — proving the framework generalizes to a second, structurally different plugin with no changes to the framework itself.
- [x] `curriculum_british` — reads "EYFSStage, KeyStage, YearGroup (curriculum-specific class-grade equivalents, map to ClassGrade)" as **one model** (`YearGroup`), not three tables — the docs give this trio one collective description, not a per-item field list the way CBC's five models each got, and Key Stages (EYFS, KS1–5) are a fixed, universally standard structure, not institution-invented content, so `key_stage` became a plain `TextChoices` field — the same "one model with a discriminator" reading `curriculum_844`'s four exam types got, applied consistently to genuinely ambiguous doc phrasing rather than guessed fresh each time. `Subject` mirrors the other two plugins' specialize-via-FK shape, plus `qualification_level` (NONE/IGCSE/A_LEVEL — IGCSE specifically, matching the British-*international*-school framing everything else in this project has, not domestic GCSE). `Coursework` gained a `max_score` field **not in the doc's literal list** (only "score" is named) — a deliberate, flagged addition, since a raw score with no denominator can't be aggregated into a mean/grade. `PredictedGrade` is the one genuinely new wrinkle: scoped to `academic_year`, not `term` (unlike every other assessment model built so far) — a teacher's standing judgment for UCAS/university applications, set directly via `services.set_predicted_grade` (`update_or_create`-keyed, `set_by` injected server-side from `request.user.id`, verified live it never trusts a client-supplied value), not derived from `Coursework`.

  Small addition to `classes_streams/selectors.py`: `get_term(institution, term_id)` (two lines, same shape as the existing `get_class_grade`) — needed because `generate_report_data` must resolve which `academic_year_id` a term belongs to before it can pull a student's current `PredictedGrade`s. `compute_mean_coursework_grade` is the **second** real consumer of `academics.GradingScale` (after `curriculum_844`), confirming it's a genuinely shared Layer 1 mechanism, not 844-specific — same unweighted-mean-of-percentages approach, same self-binding-for-Celery-safety shape even though nothing here runs from a task yet, kept consistent rather than special-cased.

  API: `/api/v1/curriculum-british/year-groups/` and `/subjects/` (ordinary CRUD). `/predicted-grades/` also gets a dedicated endpoint (no cross-curriculum equivalent) but writes route through `services.set_predicted_grade` rather than plain `ModelViewSet` create — the unique constraint means a second `POST` for the same student+subject+year must update in place, not 500 on an `IntegrityError`; same `create`/`update` → shared-service-call pattern `timetable.SubjectSlotAssignmentViewSet`/`attendance.AttendanceRecordViewSet` already established. `PredictedGrade` reads are open to any active member (not object-scoped at this endpoint) — matches `curriculum_cbc.Project`'s precedent exactly rather than inventing new object-scope logic for one model; a parent wanting to see predicted grades goes through the already-object-scoped generic report-card endpoint, where this data already appears. **No `Coursework` CRUD endpoint** — recording goes through the same generic `/api/v1/assessments/` endpoint the other two plugins use.

  **Real bug caught by `spectacular --fail-on-warn`, not by tests**: `curriculum_844` and `curriculum_british` both named their reference-data serializer `SubjectSerializer` — drf-spectacular names OpenAPI schema components after the *class name*, not the app, so this was a genuine schema-generation collision ("2 components with identical names... this will very likely result in an incorrect schema"), not a cosmetic warning. Fixed by renaming both to app-prefixed names (`EightFourFourSubjectSerializer`, `BritishSubjectSerializer`) — a small retroactive touch to the already-committed `curriculum_844` app, justified because it's a correctness fix, not a behavior change. Worth remembering for `curriculum_tvet`/`curriculum_university`: any serializer name reused across plugins needs the same app-prefix treatment before it ships.

  `.importlinter` gained a `curriculum-british-dependencies` layers contract (identical shape to the other two), `apps.curriculum_british` added to `no-layer1-imports-curriculum-plugins`'s `forbidden_modules` and to `curriculum-plugins-independent`.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (422/422 across every app, live-Postgres+Redis), `lint-imports` fully fresh (10 contracts, all kept), `manage.py spectacular` (0 warnings/errors, after the `SubjectSerializer` naming fix above), and a live end-to-end run: a teacher creates a `YearGroup` and an IGCSE `Subject` → records `Coursework` through the *generic* `/api/v1/assessments/` endpoint, confirming it resolves to `BritishEngine` → sets a `PredictedGrade` (confirmed `set_by` matches the authenticated user, never client-supplied) → the *generic* `/api/v1/report-cards/` endpoint returns the coursework history, the resolved mean grade (85→A from the seeded `GradingScale`), and the predicted grade together → the student's guardian sees the same report (200) → a made-up student id is a clean 404, not a crash.
- [x] `curriculum_tvet` — the most hierarchical plugin so far: `TVETDepartment` → `Course` (real intra-app FK) → `CompetencyUnit` (real intra-app FK), three genuine levels deep. **The first plugin whose top-level entity is not documented as specializing `academics.SubjectCatalog`** — every other plugin's equivalent (`LearningArea`/`Subject`/`Subject`) explicitly did; `Course` respects that absence rather than forcing the pattern, since vocational courses aren't "subjects" in the school sense. "WorkshopAssessment, PracticalExam (student, competency_unit, term, score, assessor)" reads as **one model** (`PracticalAssessment`) with an `assessment_type` discriminator — the same "shared field list, no per-item breakdown" reading applied consistently since `curriculum_844`'s four exam types and `curriculum_british`'s EYFS/KeyStage/YearGroup trio, not re-litigated each time. `max_score` again added despite only `score` being in the doc's literal list (same flagged, minimal reason as `Coursework`). `assessor_id` is a new field shape none of the first three plugins had — kept in the generic endpoint's `details` dict (client-supplied), deliberately **not** server-injected the way `PredictedGrade.set_by` was, since the assessor recording a result isn't necessarily the API caller (e.g. an admin entering it on an assessor's behalf) and injecting it would have required changing the generic `AssessmentRecordView` itself, which none of the first three plugins needed.

  `IndustrialAttachment` (placement record) and `Certificate` (staff-issued completion record, `issued_at` defaults to `timezone.now` at the model level so plain `ModelViewSet` CRUD works without custom create routing) both get their own CRUD endpoints — same non-object-scoped precedent as `curriculum_cbc.Project`/`curriculum_british.PredictedGrade` — and both surface in `generate_report_data` even though neither has an `AssessmentEngine` role of its own. No completion-eligibility check invented for `Certificate` issuance — nothing in the docs specifies one, so issuing stays a direct staff action. `compute_mean_practical_score` is the **third** real consumer of `academics.GradingScale` (after 8-4-4, British) — TVET assessment convention varies by institution (percentage-based vs. Distinction/Credit/Pass/Fail competency bands), and `GradingScale`'s generic label/min/max shape accommodates either without this plugin assuming which one an institution uses.

  API: `/api/v1/curriculum-tvet/departments/`, `/courses/`, `/competency-units/`, `/industrial-attachments/`, `/certificates/` — all ordinary CRUD. **No `PracticalAssessment` CRUD endpoint** — recording goes through the same generic `/api/v1/assessments/` endpoint every other plugin uses. Serializer names checked against the other three plugins' names before writing them (the `SubjectSerializer` collision `spectacular --fail-on-warn` caught last round) — confirmed clean this time, no repeats.

  `.importlinter` gained a `curriculum-tvet-dependencies` layers contract (identical shape to the other three), `apps.curriculum_tvet` added to `no-layer1-imports-curriculum-plugins`'s `forbidden_modules` and to `curriculum-plugins-independent`.

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (456/456 across every app, live-Postgres+Redis), `lint-imports` fully fresh (11 contracts, all kept), `manage.py spectacular` (0 warnings/errors, no naming collisions this round), and a live end-to-end run: an instructor builds a `TVETDepartment` → `Course` → `CompetencyUnit` chain → records a `PracticalAssessment` through the *generic* `/api/v1/assessments/` endpoint, confirming it resolves to `TVETEngine` → creates an `IndustrialAttachment` and issues a `Certificate` via their dedicated endpoints → the *generic* `/api/v1/report-cards/` endpoint returns the assessment history, the resolved mean grade (75→Credit from the seeded `GradingScale`), the attachment, and the certificate together → the student's guardian sees the same report (200) → a made-up student id is a clean 404, not a crash.
- [ ] `curriculum_university` — deferred, follow-up pass
- [x] Verify: none of Layer 1/3 code imports a `curriculum_*` app directly (only through the resolver) — a new `forbidden` contract (`no-layer1-imports-curriculum-plugins`) encodes this as an enforced rule rather than a manual check, and holds with zero `ignore_imports` exceptions since the registry pattern means `academics` genuinely never imports `curriculum_cbc`/`curriculum_844`/`curriculum_british`/`curriculum_tvet` anywhere, including its own tests (a throwaway dummy engine registered inline in the test file stands in for a real curriculum plugin, keeping `academics`' test suite fully decoupled from any plugin's existence).

  Verified via ruff, `manage.py check`, `makemigrations --check` (clean before and after `migrate` on a fresh DB), `pytest` (356/356 across every app, live-Postgres+Redis), `lint-imports` fully fresh (7 contracts, all kept — one new `curriculum-cbc-dependencies` layers contract mirroring `academics-dependencies`'s shape, one new `forbidden` contract), `manage.py spectacular` (0 warnings/errors), and a live end-to-end run: an authenticated teacher creates a `LearningArea`/`Competency` via the CBC-specific endpoints → records an assessment through the *generic* `/api/v1/assessments/` endpoint, confirming it resolves through the registry to the real `CBCEngine` → the student's own guardian pulls the report card via the *generic* `/api/v1/report-cards/` endpoint and sees it (200) → a different parent is denied (403, `PermissionDenied`) → a student with no active enrollment for that term is a clean 404, not a crash — all over real HTTP, not just the Django test client.

### Milestone

- [ ] EduCore supports multiple education systems through interchangeable curriculum plugins without modifying the core platform

---

## Phase 4 — Finance

- [ ] `finance` — `FeeStructure`, `Invoice`, `Payment`, `Receipt`, `InstallmentPlan`, `Scholarship`, `Payroll`, `ExpenseRecord`
- [ ] M-Pesa payment integration
- [ ] Bank payment recording
- [ ] Cash payment recording
- [ ] Financial reports
- [ ] Audit-log signals confirmed firing on every finance write (compliance-sensitive per `docs/architecture.md` §6)

### Milestone

- [ ] Schools can fully manage their financial operations

---

## Phase 5 — Communication

- [ ] `communication` — `Announcement`, `Circular`, `MessageThread`; `services.publish_announcement` fans out via `notifications_core`
- [ ] SMS backend (e.g. Africa's Talking) live in `notifications_core`
- [ ] Email backend live
- [ ] Push notification framework
- [ ] Notification scheduling
- [ ] Message templates

### Milestone

- [ ] Real-time communication across the institution

---

## Phase 6 — Operations

- [ ] `library` — Book, Copy, Loan, Reservation, Fine
- [ ] `inventory` — Asset, StockItem, StockMovement, Supplier
- [ ] `clinic` — HealthRecord, ClinicVisit, Medication (nurse-role-restricted selectors)
- [ ] `documents` — Document, DocumentCategory, generic-FK attach point

### Milestone

- [ ] Administrative departments become fully digitized

---

## Phase 7 — Campus Services

- [ ] `transport` — Vehicle, Route, Stop, TransportAssignment
- [ ] `hostel` — Hostel, Room, BedAllocation

### Milestone

- [ ] Complete campus management capabilities

---

## Phase 8 — Analytics & Reporting

- [ ] `analytics` — Celery-driven rollups (attendance rate, fee collection %, mean grade trends), read-only against Layer 1/2
- [ ] `reports` — PDF generation (WeasyPrint or similar), stores via `documents.services.attach`
- [ ] `dashboard` — per-portal aggregate facades (Principal, Teacher, Parent, Student dashboards)
- [ ] Exports: PDF, Excel, CSV

### Milestone

- [ ] Institution leadership gains actionable insights through analytics and reporting

---

## Phase 9 — AI Platform

- [ ] `ai_gateway` — `AIProvider` ABC, settings-driven provider toggle
- [ ] Lesson plan generation
- [ ] Exam generation
- [ ] Automatic report comments
- [ ] Student performance prediction
- [ ] Attendance risk detection
- [ ] Verify: no `curriculum_*` or `reports` code talks to an AI provider directly — only through `ai_gateway`

### Milestone

- [ ] AI becomes a native capability across EduCore

---

## Phase 10 — Enterprise Platform

- [ ] White-labeling (custom branding, themes, logos)
- [ ] Billing — subscription management, plan management, invoicing, payment gateway integration
- [ ] Dedicated-infra tenant provisioning as a scriptable, repeatable operation (per `docs/architecture.md` decision log — not a one-off manual process)
- [ ] Dedicated-database deployments
- [ ] Regional hosting
- [ ] Marketplace — curriculum plugins, third-party integrations, extensions
- [ ] Custom domains
- [ ] SSO
- [ ] Backup & disaster recovery automation
- [ ] Monitoring
- [ ] Audit compliance tooling

### Milestone

- [ ] EduCore becomes a production-ready enterprise SaaS platform capable of serving thousands of institutions across multiple regions
