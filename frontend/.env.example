# Vite exposes only VITE_-prefixed vars to client code. Kept minimal —
# most configuration (API base path) is derived from same-origin routing
# via Nginx, not an env var, per docs/frontend-architecture.md §7.
VITE_APP_NAME=EduCore
