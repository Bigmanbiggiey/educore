app_name = "v1"

urlpatterns = [
    # Per-app routers are included here as each app is implemented, e.g.:
    #     path("students/", include("apps.students.urls")),
    #
    # Auth endpoints (login/refresh/logout/me) land in Phase 1 alongside
    # apps.accounts / apps.permissions — see docs/authentication.md §3.
]
